# Migrations Scope

Current app runtime depends on:
- `017_bot_tracking_tables.sql`

Historical migrations `001` through `016` are retained for prior data systems and project history.
They are not required for the active trader/playground runtime flow.

